# ADR-0003: 地域パッケージを 2 次メッシュ単位で分割し、道路グラフを SQLite 形式で配布する

- ステータス: Accepted
- 日付: 2026-07-18
- プロジェクト: tendenko

## コンテキスト

データパイプライン (pipeline/) が生成する地域パッケージについて、(1) 地域分割の単位と (2) 道路グラフのフォーマットを決める必要がある。制約は要件定義書から:

- FR-02/03: ローリングキャッシュ — 現在地周辺の自動 DL と、significant location change での移動先先読み。分割単位は移動に追従して数個ずつ落とせる粒度が必要
- FR-04: 全国分の最小データセット (浸水想定ポリゴン + 避難場所座標 + 粗密度 DEM) < 50MB を同梱
- FR-12: 経路コストは浸水リスク・標高勾配・曲がり回数を重み付けする**カスタムコスト関数**。重みはチューニング対象であり、**探索時に端末側で動的に計算する**
- FR-15: 詳細データ未取得地域は縮退モード (コンパス案内) で成立する
- NFR-03: コールドスタートで現在地から経路確定まで < 5 秒 (端末内計算)
- NFR-04: 1 地域パッケージ < 150MB / 同梱最小データセット < 50MB
- 要件 §6: 端末ストレージ層は GRDB (SQLite) / MBTiles を想定済み

FR-12 の帰結として、**コスト関数を前処理で焼き込む方式 (OSRM の Contraction Hierarchies 等) は前提から不適合**。グラフは生の隣接構造 + 静的属性で持ち、コストは探索時に計算する。

## 決定

### 1. 分割単位: 2 次メッシュ (JIS X 0410、約 10km 四方)

- メッシュコード (例: `5339-24`) をパッケージ ID とする。緯度経度から機械的に算出でき、境界ポリゴンデータが不要
- **フルパッケージを生成するのは沿岸メッシュのみ** — 津波浸水想定区域と交差するメッシュ + 海岸線から一定バッファ内のメッシュ (閾値はパイプライン実装時に実測して決める)。内陸は同梱最小データセット + 縮退モード (FR-15) で成立する
- ローリングキャッシュは現在地メッシュ + 周囲 8 メッシュ (3×3) を保持。登録地点 (FR-05) も同様に 3×3 で常時保持
- パッケージ一覧・バージョン・ハッシュ・生成日時は manifest (JSON) で GCS に置き、FR-06 の差分 DL と「6 ヶ月超は警告」(要件 §8) の判定に使う

### 2. 道路グラフ: SQLite (1 メッシュ 1 ファイル) + 端末でメモリ展開

パイプライン (Go) が `region-<meshcode>.sqlite` を生成する:

| テーブル | 内容 |
|---|---|
| `nodes` | id (OSM 由来のグローバル ID), lat, lon, 標高 (DEM から焼き込み) |
| `edges` | from, to, 距離, 標高勾配, 方位角, 属性フラグ (浸水想定区域内・階段・私道・橋 等) |
| `shelters` | 指定緊急避難場所 (災害種別: 津波) の座標・名称・標高 |
| `meta` | メッシュコード, 生成日時, ソースデータのバージョン, スキーマバージョン |

- 静的属性 (浸水ポリゴンとの交差判定、DEM との結合) は**パイプライン側で焼き込む**。端末は幾何演算をしない
- 端末 (Swift) は GRDB で読み込み、現在地周辺メッシュ (3×3) の隣接リストをメモリ展開してドメイン層の A* に渡す。コスト関数は探索時に属性から動的計算 — ドメイン層は「展開済みグラフ → 経路」の純粋関数として TDD できる
- メッシュ跨ぎ: エッジは始点ノードが属するメッシュに収載する。ノード ID がグローバルなので、3×3 の読込結果を単純結合すれば境界を跨ぐグラフになる
- 地図タイルは同じメッシュ単位の MBTiles (tilemaker 生成)。パッケージ = `region-<meshcode>.sqlite` + `tiles-<meshcode>.mbtiles`

## 検討した選択肢

### 分割単位

**市区町村** — ✅ 避難場所データ (自治体単位で整備) と整合し人間に分かりやすい。❌ 境界ポリゴンが必要で合併により変化する。面積のばらつきが極端 (数 km² 〜 1,500km² 超) で、NFR-04 を分割単位側で保証できず、大きい市は結局内部分割する二重構造になる。**棄却**

**1 次メッシュ (約 80km 四方)** — ✅ パッケージ数が少なく管理が楽。❌ 都市部で道路グラフ + 地図タイルが 150MB をほぼ確実に超過し NFR-04 と衝突。先読みの 1 単位も重い。**棄却**

**3 次メッシュ (約 1km 四方) / 可変クアッドツリー** — ❌ 3 次は粒度過剰でパッケージ数とリクエスト数が爆発する。可変分割はサイズ効率が最良だが、分割ロジック自体が保守対象になり個人開発の運用時間と相性が悪い。**棄却** (都市部の 2 次メッシュが 150MB を超える事態が実測で判明したら再検討)

### 道路グラフフォーマット

**自前バイナリ (FlatBuffers 等の mmap 形式)** — ✅ 読込は最速。❌ スキーマ進化・破損調査・中身確認のツールをすべて自作し、Go (書き手) と Swift (読み手) の両方で保守する。NFR-03 の 5 秒は SQLite でも余裕で満たせる見込みのため過剰最適化。**棄却** (実測で SQLite が 5 秒を脅かす場合のみ再検討)

**Valhalla タイル** — ✅ 実績あるエンジンで動的コスティング機構もある。❌ C++ ライブラリの iOS 組み込みが重く、浸水リスク × 標高勾配の独自コストはエンジン内部への改修が必要。ドメイン層を純粋 Swift + TDD で書く方針 (CLAUDE.md) と衝突。**棄却**

**OSRM / Contraction Hierarchies 系** — ❌ コスト関数を前処理で焼き込む前提が FR-12 (動的コスト) と根本的に不適合。**即棄却**

## 正直な弱点

- **SQLite 読込 + メモリ展開のコールドスタート性能は未実測**。NFR-03 (5 秒) に対し、app 側の実装初期にベンチマークを書いて検証する。超過する場合は展開範囲の縮小 (3×3 → 現在地 + 進行方向のみ) から先に試す
- 都市部 2 次メッシュのパッケージサイズも未実測。パイプラインの垂直スライス実装で最初に確認する
- 沿岸メッシュは千数百個規模になる見込みで、manifest と GCS レイアウトの設計が必要 (パイプライン実装時)
- 道路グラフは OSM の派生データベースであり、**公開配布時の ODbL share-alike 義務の整理が未決のまま残る** — [ADR-0002](0002-oss-licensing.md) / [docs/licenses.md](../licenses.md) で追跡

## 検証記録 (2026-07-18 垂直スライス実測)

実在の 2 メッシュで region.sqlite を end-to-end 生成した (OSM 東北抽出版 + 地理院 DEM10B、pipeline/cmd/build-package)。

| メッシュ | 地域 | ノード | エッジ | region.sqlite | 生成時間* |
|---|---|---|---|---|---|
| 584177 | 釜石市中心部 (沿岸・地方) | 8,397 | 8,729 | **0.9 MB** | 3.5 秒 |
| 574036 | 仙台市中心部 (都市・高密度) | 61,198 | 70,061 | **7.4 MB** | 8.1 秒 |

*生成時間の大半は DEM タイル初回取得 (ネットワーク)。キャッシュ後は 1 秒未満。osmium のメッシュ切り出しは東北全域 291MB からで約 3 秒。

**結論: 道路グラフは NFR-04 (150MB) に対して支配項ではない** (都市部最悪級でも 1 桁 MB)。パッケージサイズの支配項は地図タイル (MBTiles) になる。

**未完了**: MBTiles の実測は nixpkgs の tilemaker 3.1.0 が macOS/arm64 でクラッシュするため保留。本番パイプラインは Cloud Run jobs (Linux) で動かすため、Linux 環境での実測が必要。浸水域フラグ (A40) と避難場所 (shelters) の取り込みも未実装。

### 追記 (2026-07-19): 東北全域の一括生成

海岸線からの沿岸メッシュ自動列挙 (バッファ 1 リング) + 一括生成を東北全域で実行した。

- 対象 489 メッシュ (海岸線 235 + バッファ) → **生成 309 パッケージ / スキップ 180 (海上等の空メッシュ)**
- 合計 **411MB**、中央値 1.1MB、最大 10.6MB (574037 = 仙台北東部)。**全メッシュが NFR-04 (150MB) に 1 桁以上の余裕**
- 所要 695 秒 (DEM 大半キャッシュ済み)。初回は DEM 取得律速で約 2 倍
- 教訓: 長時間バッチでは DEM タイル取得の一時的な connection reset が実際に発生する。指数バックオフのリトライ (4 回) を dem クライアントに実装済み

### 追記 (2026-07-20): 全国一括生成の実測

japan-latest.osm.pbf (2.3GB) から全国の沿岸パッケージを一括生成した。

- 海岸線メッシュ 1,806 + バッファ = 対象 4,481 メッシュ → **生成 2,515 パッケージ / スキップ 1,966 (海上等)**
- 合計 **4.55GB** (GCS 側の総量。端末が持つのは 3×3 + 登録地点のみ)、総ノード数 3,950 万
- 中央値 1.1MB、**最大 26.5MB (533946 = 東京都心)**。全パッケージが同梱最小データセット制約 (50MB) すら下回り、**NFR-04 (150MB) は全国で成立**
- 所要 2.6 時間 (DEM 初回取得が律速。タイルキャッシュ済みなら大幅短縮)
- 教訓: 4,481 メッシュの直接 bbox 抽出は osmium が OOM kill される。1 次メッシュ (80km) 単位で
  20 個ずつ切り出してから配下の 2 次メッシュを展開する 2 段階方式で解決した

### 追記 (2026-07-19): 端末側読込 + 探索の実測 (NFR-03)

GraphLoader (GRDB) + EvacuationRouter (ターン抑制付き Dijkstra、動的コスト) を実装し、実パッケージで計測 (macOS, arm64):

| パッケージ | ノード | 読込 | 探索 (最遠点) |
|---|---|---|---|
| 釜石 584177 (0.9MB) | 8,397 | 0.03 秒 | 0.004 秒 |
| 仙台北東 574037 (10.6MB, 最大) | 85,492 | 0.33 秒 | 0.49 秒 |

**「SQLite + メモリ展開」で NFR-03 (< 5 秒) に 1 桁の余裕**があり、自前バイナリ形式の再検討は不要と判断。実機 (iPhone) と 3×3 結合での再計測は app 実装時に行う。

### 追記 (2026-07-20): 浸水想定区域フラグ・避難場所テーブルの実装

FlagInundation の付与と shelters テーブルの実装を行った。実データ (国土数値情報 A40・国土地理院の指定緊急避難場所) の**取得元 URL はまだ確認していない** — ダウンロード URL を推測で使わない方針のため、次のいずれかをユーザーに確認してから着手する: (a) 具体的な配布 URL、(b) 既にローカルにある元データファイル。

そこで pipeline は**正規化 GeoJSON を受け取る形**で実装した。実データ取得後は ogr2ogr 等で以下の形式に変換するだけで使える。

- `internal/inundation`: Polygon/MultiPolygon の FeatureCollection (座標は GeoJSON 標準の `[lon, lat]`)。穴 (内水面など) は内側リングとして表現でき、外周とのレイキャストを XOR することで正しく除外する
- `internal/shelterdata`: Point の FeatureCollection、`properties.tsunami == true` の地物のみ採用 (FR-04 の「災害種別: 津波」)。`properties.name` を避難場所名とする

`build-package` に `-inundation <path>` `-shelters <path>` を追加 (両方省略可。省略時は従来どおり FlagInundation なし・shelters 空)。

**交差判定の設計判断**: エッジ (2 次メッシュ内の短い道路区間) の両端点+中点をポリゴンに対してテストする近似方式にした。完全な線分-多角形交差判定 (Sutherland–Hodgman 等) は実装コストに対して、この用途 (エッジは短く、浸水域は広い連続領域) ではオーバースペックと判断。ポリゴン数が多くなる想定 (全国の A40 データ) に備え、ポリゴンごとの bbox 前置フィルタを Index に持たせている。

**動作確認**: 釜石メッシュ (584177, 8,397 ノード) に合成の浸水ポリゴンと避難場所 (圏外 1 件を含む) を与えて実行。8,729 エッジ中 1,381 エッジに FlagInundation が付与され、圏外の避難場所は shelters テーブルから正しく除外されることを確認した。

### 追記 (2026-07-20): 実データの取得元と全国投入

浸水想定区域・避難場所とも、実データの取得元を確認できたため取り込んだ (取得は WebSearch/WebFetch で公式配布ページを特定し、URL を推測せず確認してから行った)。

- **浸水想定区域**: 国土数値情報 津波浸水想定データ (A40)。都道府県別 Shapefile、44 都道府県分 (北関東・内陸県など沿岸を持たない県は対象外)、合計約 1.7GB。配布元: https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-A40-v2_1.html (各県 ZIP への実 URL はページの JS 内に埋め込まれており、`../data/A40/A40-XX/A40-XX_YY_GML.zip` 形式)
- **避難場所**: 国土地理院「指定緊急避難場所データ」の全国 GeoJSON (G空間情報センター配信、CC BY 4.0)。115,447 件中、「津波」列が該当する 40,393 件を採用 (FR-04)。配布元: https://www.geospatial.jp/ckan/dataset/hinanbasho

**ETL**: 実データは 1 都道府県あたり数万件の depth-band ポリゴンに分かれており、そのまま Index に載せるとファイルサイズ・メモリともに肥大化する (実測: 青森県のみで生ポリゴン 82,792 件・56MB)。このパイプラインはエッジが浸水想定区域に触れるかの boolean 判定にしか使わないため、`ogr2ogr` の `ST_Union` で都道府県ごとに 1 つの MultiPolygon へ dissolve し、`-simplify 0.00003度` (≈3m、エッジの端点+中点サンプリング近似の誤差予算内) で単純化してからマージした。結果、全国 44 都道府県で 808MB (dissolve 後も 387,395 個の非連結ポリゴン片、頂点数 946 万)。変換スクリプトは `pipeline/scripts/normalize-a40.sh` / `normalize-shelters.sh` としてリポジトリに残し、再現可能にしている (元データそのもの・変換後の GeoJSON は `pipeline/data/` 配下で gitignore 対象)。

**性能問題と対処**: 全国 Index (387,395 ポリゴン) をそのまま釜石メッシュ (8,729 エッジ) に適用したところ 3.49 秒かかった。エッジごとに 3 点 (両端点+中点) サンプルし、そのたびに全国の全ポリゴンの bbox を線形走査していたため。`Index.Subset(bbox)` を追加し、メッシュ処理の最初に全国 Index を対象メッシュ範囲へ 1 回だけ絞り込んでから edge ループに使うよう変更したところ、**3.49 秒 → 0.02 秒 (175 倍)** に改善した。全国 2,515 パッケージへの適用でも無視できるオーバーヘッドに収まる見込み。

**動作確認 (実データ)**: 釜石メッシュで 8,729 エッジ中 **4,022 エッジ (46%)** に浸水フラグが付与された。2011 年の津波で甚大な被害を受けた沿岸市街地であることと整合する。避難場所は 46 件(「鵜住居小学校・釜石東中学校校庭」等、実在の高台避難所)。

**ライセンス**: A40 は都道府県ごとに利用条件が異なり (大多数はオープンデータだが一部条件付き)、避難場所データは CC BY 4.0。[docs/licenses.md](../licenses.md) に記録した。全国パッケージを公開配布する場合は ADR-0002 (ライセンス未決) とあわせて都道府県別の利用条件を再確認すること。

**既知のデータ欠落 (重要)**: 国土数値情報 A40 は 47 都道府県中 37 都道府県分しか提供されていない。栃木・群馬・埼玉・山梨・長野・滋賀・奈良は内陸県で津波リスクがないため欠落は妥当だが、**東京都・福井県・香川県は沿岸県にもかかわらず A40 データが存在しない** (配布元ページに記載なし)。これらの地域では道路・避難場所データと標高ベースの経路コスト (登り優先) は機能するが、FR-12 の浸水想定区域による経路重み付けだけが効かない。都道府県独自のハザードマップ (東京都防災アプリ等) からの補完は将来課題として残す。

### 追記 (2026-07-20): 全国再生成の最終結果

浸水フラグ・避難場所を組み込んだ状態で全国 pbf から再生成した (`make pipeline-run`、Subset 最適化込み)。

- **2,515 パッケージ / 合計 4,557MB** (浸水フラグ・避難場所データ追加前の 4,553MB からほぼ変わらず — フラグは 1 ビット、shelters もメッシュあたり数十件程度でサイズへの影響は無視できる)
- 所要 **61 分** (DEM がほぼキャッシュ済みのため、初回の 2.6 時間から大幅短縮)
- 最大パッケージは引き続き東京都心 533946 (26.5MB)、**全パッケージが NFR-04 (150MB) を大きく下回る**
- 避難場所は全国で **39,756 件** (メッシュ境界をまたぐ避難場所は隣接メッシュに重複収載されるため、正規化データの 40,393 件より若干少ない実配置数になる)
- 実例: 釜石 (584177) 8,729 エッジ中 4,022 (46%) に浸水フラグ、避難場所 46 件。高知市 (503324) 106,979 エッジ中 56,248 (53%) に浸水フラグ、避難場所 364 件 (南海トラフ想定地域として高い割合が妥当)。東京都心 (533946) は A40 データ欠落により浸水フラグ 0 件 (上記の既知の欠落を参照)。避難場所は 2 件 (佃島小学校・佃中学校) で、これは正規化データを独立に bbox フィルタしても同じ結果になることを確認済み — このメッシュが中央区佃島の湾岸部を指しており、その地域では実際にこの 2 施設のみが津波指定避難場所として登録されているため (バグではなく実データの反映)

### 追記 (2026-07-20): MBTiles 生成の Linux 検証とパイプライン組み込み

macOS/arm64 での nixpkgs tilemaker クラッシュが実行環境固有の問題か確認するため、Docker Desktop 経由でローカルに Linux コンテナ (`nixos/nix:latest`、`nix shell nixpkgs#tilemaker` で同一パッケージ) を立てて同じコマンドを実行した。**Linux では問題なく動作**(釜石メッシュ 0.9 秒・576KB、仙台中心部 1.8 秒・2.8MB)。これで tilemaker のクラッシュは macOS/arm64 固有の nixpkgs ビルド問題であり、本番パイプライン (Cloud Run jobs = Linux) には影響しないと確定した。

これを踏まえ `build-package` に `-tiles` フラグを追加し、MBTiles 生成をパイプライン本体に組み込んだ:

- `extractAll` のレベル 2 (1 次メッシュ→2 次メッシュ展開) で、tiles 有効時は同じ範囲の `.osm.pbf` も併せて出力する (osmium は 1 回の入力読み込みで複数出力を書けるため、追加コストはほぼゼロ)
- `buildOne` は `tilemaker` が PATH にあれば `tiles-<mesh>.mbtiles` を生成し、manifest.json に `tiles_file` / `tiles_bytes` を記録する。`-tiles` 未指定時や `tilemaker` が無い環境 (macOS ローカル) では region.sqlite の生成のみ行い、エラーにはしない (`-tiles` 指定時に限り起動時チェックで明示的に fail する)
- ローカル (macOS) で単一メッシュの MBTiles を試したいときのために `pipeline/scripts/tilemaker-docker.sh` と `make pipeline-tiles-one` を用意した (Docker の Linux コンテナ経由。本番では不要)
- **動作確認 (Linux コンテナ + クロスコンパイルした pipeline バイナリでの e2e)**: 一括モードで 5 パッケージ生成、region.sqlite (計 1.0MB) + tiles (計 1.8MB) が manifest.json に正しく記録されることを確認

MBTiles を含めても NFR-04 (150MB) への影響は無視できる (仙台中心部で region.sqlite 7.4MB + tiles 2.8MB = 10.2MB)。全国 2,515 パッケージへの本番適用 (Cloud Run jobs) は次のインフラ構築セッションで行う。

### 追記 (2026-07-25): 本番実行イメージのコンテナ化と Linux での -tiles 全工程検証

Cloud Run jobs で -tiles 付き全国 run を回すため、パイプラインを Linux コンテナ化した ([ADR-0001](0001-execution-platform.md) の Cloud Run jobs)。

- **リポジトリ直下の `Dockerfile`**: `nixos/nix` ベース。Linux コンテナ内で flake.nix (正本) から `packages.pipeline-tools` (go/osmium/tilemaker/gdal を flake.lock 固定で束ねた buildEnv) を realise し、静的な Go バイナリ (CGO 不要) を焼き込む。ビルド自体が Linux で走るためクロスコンパイルや linux-builder が不要
  - **ハマりどころ**: 当初 `nix develop` (devShell) で go を得ようとしたが、devShell が `xcodegen` (macOS 専用パッケージ) に依存するため `aarch64-linux` で評価不能だった。go を pipeline-tools 側へ移して解決
- **検証 (Docker Desktop の Linux コンテナ)**: `mesh-584177.osm.pbf` で -tiles 付き一括モードを実行し、**3 パッケージすべてで region.sqlite + tiles.mbtiles + manifest.json が生成される**ことを確認 (manifest に tiles_file/tiles_bytes/tiles_sha256 も記録)。これで macOS の tilemaker クラッシュは本番 (Linux) で解消されることを実行環境ごと確定した
- **既知の見栄えギャップ**: tilemaker は海岸線 (OSM water_polygons) と landcover (Natural Earth) の外部シェープファイルを任意で読む。コンテナに同梱していないため警告が出るが、OSM 由来の地物からタイルは生成される (道路・水域は描画される)。海岸線/landcover のベースレイヤが必要になったら、これらのシェープをイメージへ追加する
- **infra**: `infra/pipeline.tf` に Cloud Run v2 job + Cloud Scheduler + データバケット + Artifact Registry + SA/IAM を定義 ([ADR-0001](0001-execution-platform.md) Stage 1)。イメージ push と `tofu apply`、全国 run は GCP 認証が要るため別途

### 追記 (2026-07-20): app/ でのオフライン地図表示を実装・実機確認

MapLibre Native が MBTiles をネットワーク越しに取得する前提の設計であることを踏まえ、TendenkoStorage に **ローカル HTTP サーバー方式**で組み込んだ。

- `MBTilesReader`: MBTiles 仕様の `tile_row` が TMS 方式 (南→北) で、XYZ 方式 (北→南、地図ライブラリの標準) と Y が反転していることを吸収する。GRDB でクエリするだけの薄い読み出し層
- `MBTilesServer`: `Network.framework` の `NWListener` で 127.0.0.1 上に最小 HTTP サーバーを立て、`/{z}/{x}/{y}.pbf` へのリクエストを `MBTilesReader` に橋渡しする。**通信は一切発生しない完全ローカルな実装**。MapLibre のスタイル JSON にはこのサーバーの URL (`http://127.0.0.1:<port>/{z}/{x}/{y}.pbf`) を vector source として渡す
- `MapView` (SwiftUI): `MLNMapView` (MapLibre Native) を `UIViewRepresentable` でラップ

**ハマりどころ (実機検証で発覚)**: tilemaker が生成する MBTiles の `tile_data` は **gzip 圧縮されたまま**格納されている (マジックバイト `1F 8B` で確認)。ローカルサーバーが `Content-Encoding: gzip` ヘッダを付けずに生バイト列を返すと、MapLibre 側はエラーを出さずに圧縮バイト列をそのまま MVT protobuf として解釈しようとし、**地物 0 件の空タイルとして静かに失敗する** — スタイルの背景色だけが表示され、道路や水域が一切描画されない状態になり、原因の切り分けに時間がかかった (ネットワークログでは 200 OK で正常にタイル取得できているように見えるため、一見データ取得の問題に見えない)。`MBTilesServer.isGzip` でマジックバイトを検出し、該当時のみ `Content-Encoding: gzip` を付与することで解決。URLSession 側の透過的な解凍に任せている

**動作確認 (iOS シミュレータ実機)**: 釜石メッシュ (584177) の実データ (region.sqlite + tiles-584177.mbtiles、パイプラインで生成した実物) をアプリにバンドルし、iPhone 17 シミュレータで起動 → スクリーンショットで実際の道路網・海岸線の描画を確認した。開発用の一時的な同梱データであり、本来は FR-02 (地域パッケージの自動ダウンロード) で取得する — 未実装

## 帰結

- pipeline/cmd/build-package は「対象メッシュ列挙 → osmium 抽出 → グラフ構築 + DEM/浸水域焼き込み → region.sqlite + tiles.mbtiles + manifest 生成 → GCS」の構成になる
- 次の実装: **1 メッシュを end-to-end で生成する垂直スライス** (実在の沿岸メッシュ 1 個で region.sqlite を作り、サイズと生成時間を実測する) — 完了 (2026-07-18 の追記参照)
- app 側は GraphLoader (SQLite → 隣接リスト) と EvacuationRouter (A* + 動的コスト) をドメイン層 TDD で実装する — 完了 (2026-07-19 の追記参照)
- **未解決**: 浸水想定区域 (国土数値情報 A40) と指定緊急避難場所 (国土地理院) の実データ取得元。次セッションでユーザーに確認する

## 追記 (2026-07-25): 東京都・福井県・香川県 (A40 欠落県) の独自データ公開状況の調査

上記「既知のデータ欠落」で残した、国の A40 に無い沿岸 3 県について、各県が独自に公開する津波浸水想定データの有無・形式・ライセンスを一次情報で確認した (URL は推測せず公式ページで確認)。

| 県 | 独自の津波浸水想定 | 形式 | ライセンス | 補完可否 |
|---|---|---|---|---|
| **福井県** | あり (県オープンデータポータル) | **ZIP (機械可読 GIS) 約 1.5MB** | **CC BY 4.0** | **補完可能** |
| 東京都 | あり (島しょ地域中心) | PDF のみ | 記載なし | 不可 (要問い合わせ/デジタイズ) |
| 香川県 | あり (市町別) | PDF のみ | 不明 | 不可 (要問い合わせ/デジタイズ) |

- **福井県**: [県オープンデータ](https://www.pref.fukui.lg.jp/doc/dx-suishin/opendata/list_1.html) に「津波浸水想定」ZIP と「津波災害警戒区域」ZIP が **CC BY 4.0** で公開されている ([津波災害警戒区域の指定について](https://www.pref.fukui.lg.jp/doc/sabo/tsunamisaigaikeikaikuiki.html) からリンク)。A40 と同じく `ogr2ogr` で `internal/inundation` の正規化 GeoJSON (Polygon/MultiPolygon, `[lon,lat]`) に変換すれば取り込める。ZIP の中身 (shp か) はダウンロード後に要確認。避難場所は全国 GeoJSON (国土地理院) に含まれるため福井分も既にある
- **東京都**: [東京都防災 (津波防災地域づくり法に基づく津波浸水想定)](https://www.bousai.metro.tokyo.lg.jp/taisaku/torikumi/1000216/1023301/1023364/index.html) は**島しょ地域中心の PDF 図面のみ**。機械可読 GIS の配布・オープンデータ表記なし。湾岸部の陸域は南海トラフ/首都直下の被害想定側にあるが、津波浸水想定としての GIS 配布は確認できず
- **香川県**: [香川県 津波浸水想定図](https://www.pref.kagawa.lg.jp/kikikanri/jishin-tunami/higaisotei/w00p3b150617141706.html) は**市町別 PDF のみ**。GIS 配布・ライセンス表記なし

**結論**:

- **福井県は次に補完する** — CC BY 4.0 で機械可読。`normalize-a40.sh` と同様の変換スクリプトを足し、`-inundation` に渡す正規化データへ福井分をマージする。取得元は上記で確認済み

  **実装済み (2026-07-28)**: `pipeline/scripts/normalize-fukui.sh` を追加した。A40 との差分は 2 点: (1) shp に `.prj` が無く座標が平面直角座標系第6系 (メートル) なので `-s_srs EPSG:6674` を明示 (サンプル隅点を WGS84 変換し福井県沿岸に着地することを確認)、(2) zip 内ディレクトリ名が Shift-JIS で unzip がエンコーディング解釈に失敗するため `unzip -j` で階層を落として回避。10m メッシュの浸水深ポリゴン 72,806 件を A40 同様 ST_Union で 1 MultiPolygon に dissolve + simplify (≈3m) して 1.3MB の `data/fukui/fukui.dissolved.geojson` を生成し、`normalize-a40.sh` の最終マージが拾う (`make pipeline-normalize-data` は fukui → a40 → shelters の順)。**検証**: 敦賀市メッシュ (533630) を japan pbf から切り出して `-inundation` に福井データを渡したところ 18,355 エッジ中 177 に浸水フラグが付与され、その端点が緯度 35.653–35.665・経度 136.062–136.073 (敦賀港・湾奥の低地) に集中することを確認した
- **東京都・香川県は当面据え置き** — 機械可読データが公開されておらず、PDF からのデジタイズや県への個別問い合わせが必要でコストが高い。これらの地域では引き続き標高ベースの経路コスト (登り優先) で縮退動作する (FR-15)。将来、県が GIS を公開するか、PDF ジオリファレンス + ベクタ化に着手する時点で再検討
- ライセンス面: 福井 (CC BY 4.0) は避難場所データと同条件で扱える ([docs/licenses.md](../licenses.md))。[ADR-0002](0002-oss-licensing.md) の A40 都道府県別精査 (公開配布のブロッカー) とは別軸だが、福井を足す際に帰属表示の対象へ加える

### 追記 (2026-07-28): 東京都・香川県も「ハザードマップポータルサイト」経由でラスタタイルとして取得可能と判明

上記「東京都・香川県は PDF のみ (不可)」は各県自身の防災サイトを見た結果であり、不完全な調査だった。国土地理院が運営する全国横断の「重ねるハザードマップ (ハザードマップポータルサイト)」を確認したところ、**A40 に採用されていない都道府県のデータも別チャネルでラスタタイル配信されている**ことが分かった。

- URL パターン: `https://disaportaldata.gsi.go.jp/raster/04_tsunami_newlegend_pref_data/<都道府県コード>/{z}/{x}/{y}.png` (東京都=13、香川県=37)。国土地理院タイルと同じ XYZ 方式
- **実データを取得して検証済み**: 伊豆大島沿岸 (東京都、z10・z12) / 高松・坂出・観音寺沿岸 (香川県、z10・z12) のタイルが実際に色分けされた浸水深図として返ることを画像で確認した。東京都は島しょ部中心 (本土側の一部座標は 404 で対象外)、香川県は沿岸市町で広く 200 が返る
- ライセンス: **PDL1.0** (公共データ利用規約)。商用・非商用の区別なし、出典表示必須 (「出典：『ハザードマップポータルサイト』」)、**編集・加工は規約上可** (加工した旨の明記が必要)。宅地建物取引での利用のみ明確に禁止

**結論のアップデート**: 「東京都・香川県は取得不可」ではなく、正しくは「**取得は可能だが、A40/福井のような Shapefile ではなくラスタ (PNG タイル) のため、色凡例を閾値処理してポリゴン化する変換工程が別途必要**」。既存の `ogr2ogr` ベースの変換 (A40/福井) より一段重い作業になる:

1. 凡例 (浸水深カテゴリ ↔ 色) の対応表を確認する (ハザードマップポータルサイトの凡例定義から)
2. 対象タイルを必要な zoom で網羅的に取得し、色閾値処理 → 輪郭抽出でポリゴン化 (例: GDAL の `gdal_polygonize.py` を色クラスごとに適用)
3. タイル座標系 (Web Mercator) → 正規化 GeoJSON の `[lon,lat]` (WGS84) へ変換
4. ラスタ由来のため A40/福井より輪郭精度が落ちる点をコメントに明記 (規約上も加工時の精度劣化の言及が必要)

この変換パイプラインは福井の実装より複雑で、まだスコープ・実装時間を見積もっていない。**次にこの2県へ着手する際は、変換スクリプトの設計を先に詰める** (専用の ADR 追記 or 実装セッションで)。当面は他タスク (MapView 可読性向上 ADR-0006、全国 MBTiles 本番適用) を優先し、この2県は標高ベースの経路コストのみで縮退動作を継続する。

## 追記 (2026-07-29): 東京都・香川県ラスタ変換パイプラインの設計

実装に入る前段として、変換パイプラインの設計を詰めた (このセッションでは設計のみ、実装は次回)。

### 凡例 (色 → 浸水深カテゴリ) の一次情報

ハザードマップポータルサイトの [凡例説明 PDF](https://disaportal.gsi.go.jp/hazardmapportal/legend.pdf) を確認したところ、「重ねるハザードマップ」上で表示される **津波浸水想定は、洪水浸水想定区域 (想定最大規模) と同一の凡例** (「水害ハザードマップ作成の手引き」令和5年5月改定 に基づく) を使っていることが分かった。カテゴリは 6 段階:

| カテゴリ | 浸水深 |
|---|---|
| 1 | 〜0.5m |
| 2 | 0.5〜3m |
| 3 | 3〜5m |
| 4 | 5〜10m |
| 5 | 10〜20m |
| 6 | 20m〜 |

RGB 値は第三者サイト ([ton2net.com](https://ton2net.com/shinsuishin-color/)、出典として国土交通省「水害ハザードマップ作成方法」を明記) で以下の値が公開されているのを確認した:

| カテゴリ | RGB | Hex |
|---|---|---|
| 1 (〜0.5m) | 247,245,169 | `#f7f5a9` |
| 2 (0.5〜3m) | 255,216,192 | `#ffd8c0` |
| 3 (3〜5m) | 255,183,183 | `#ffb7b7` |
| 4 (5〜10m) | 255,145,145 | `#ff9191` |
| 5 (10〜20m) | 242,133,201 | `#f285c9` |
| 6 (20m〜) | 220,122,220 | `#dc7adc` |

**未確認 (実装着手時に要確認)**: 上記 RGB 表は二次情報 (ブログ記事) であり、福井県・A40 で徹底してきた「一次情報を確認してから使う」方針にまだ従えていない。実装時は国土交通省の「水害ハザードマップ作成の手引き」原本 (または disaportaldata が配信する PNG タイルの実ピクセル値そのもの) で照合してから使う。本パイプラインはブール値 (浸水想定域に触れるか否か) しか使わないため、6 色のどれかに (アンチエイリアシングによる中間色を許容する近似一致で) マッチすれば「浸水あり」と判定すればよく、カテゴリ自体の精度はそこまで重要ではない — この点は表の正確性より閾値マッチングの許容誤差設計の方が優先度が高い。

### 変換パイプライン設計 (実装対象)

A40/福井 (`normalize-a40.sh` / `normalize-fukui.sh`) と同じ最終フォーマット (都道府県ごとに dissolve した 1 MultiPolygon、`[lon,lat]`、`-simplify 0.00003度` ≈3m) に合流させる設計とする。

1. **タイル範囲の列挙**: 対象県のバウンディングボックス (東京都は伊豆諸島・小笠原諸島の島しょ部、香川県は県全体の沿岸市町) を、選定した z (前回実データ検証は z10/z12 で成功済み。解像度とタイル枚数のトレードオフは実装時に確定) での XYZ タイル座標に変換する標準の slippy-map 式で列挙する
2. **取得**: `https://disaportaldata.gsi.go.jp/raster/04_tsunami_newlegend_pref_data/<都道府県コード>/{z}/{x}/{y}.png` (東京都=13、香川県=37) から対象タイルを取得。404 (対象外セル) は「浸水なし」として無視する
3. **モザイク化**: タイル境界でポリゴンが分断されないよう、`gdalbuildvrt` 等で県内タイルを 1 枚のラスタに合成してから処理する (タイル単位で個別に polygonize しない)
4. **2値化 → ポリゴン化**: モザイクの各ピクセルを「6色のいずれかに近似一致 (許容誤差付き) すれば 1、それ以外 (透過・背景) は 0」の 2 値マスクに変換し、`gdal_polygonize.py` でポリゴン化する
5. **座標変換**: タイルの座標系 (Web Mercator, EPSG:3857) から `ogr2ogr` で EPSG:4326 (WGS84) へ再投影する (A40 の平面直角座標系変換と同じ位置づけの工程)
6. **dissolve/simplify/マージ**: A40/福井と同じ `ST_Union` dissolve + `-simplify 0.00003度` を適用し、`pipeline/data/tokyo/tokyo.dissolved.geojson` / `pipeline/data/kagawa/kagawa.dissolved.geojson` として出力。`pipeline-normalize-data` のマージ順に tokyo/kagawa を追加する

想定スクリプト名: `pipeline/scripts/fetch-tsunami-raster.sh` (タイル取得、都道府県コードをパラメータ化) + `pipeline/scripts/normalize-raster-tsunami.sh` (2値化・polygonize・座標変換・dissolve、都道府県コードと出力先をパラメータ化して東京都・香川県で共有する)。A40/福井のようにスクリプトを完全に分けるのではなく、ラスタ変換工程自体は同一ロジックなのでパラメータ化して共有する方が保守対象が増えない。

### 未解決事項 (実装時に詰める)

- **RGB 表の一次情報照合** (上述)
- **東京都の対象範囲**: 前回の実データ確認では伊豆大島沿岸で有効なタイルを確認済みだが、本土側 (東京湾岸) の一部座標は 404 だった。本土部に津波浸水想定データが元々存在しないのか、対象外セルなだけなのかは未確認。都道府県コード単位の一括取得を試して実際のカバレッジを確認する必要がある
- **色の近似一致の許容誤差**: PNG は圧縮・アンチエイリアシングで境界ピクセルが中間色になる。どの程度の距離 (色空間上のユークリッド距離など) を「一致」とみなすかは未設計。誤差を広げすぎると背景色まで誤検出し、狭すぎると境界付近のポリゴンが欠ける
- **zoom レベルの確定**: z12 で実データ取得は確認済みだが、タイル枚数 (=取得コスト) と輪郭精度のトレードオフを比較した上で確定していない
- **ライセンス**: PDL1.0 (出典表示必須、加工可・要加工明記、宅地建物取引利用のみ禁止)。ODbL (OSM由来) や CC BY 4.0 (福井・避難場所) とは条件が異なる独立した扱いになるため、`docs/licenses.md` に単独項目として追記する。ADR-0002 (公開配布の可否) との整合も実装時に再確認する
- **精度の明記**: ラスタ由来のため A40/福井 (ベクタ) より輪郭精度が低い。PDL1.0 の規約上も加工した旨の明記が必要なため、`region.sqlite` の meta か docs/licenses.md にその旨を記録する

この設計に基づく実装 (スクリプト作成・全国パイプラインへの組み込み) は次回のセッションで着手する。
